        server {
            listen 5900;
            location /index.html {
                return 200 '{\n  "nat-gateway-addr": $remote_addr,\n  "destination-port": $server_port,\n  "destination-addr": $server_addr,\n  "nginx-prxy": $host,\n  "status": "200",\n  "message": "OK - Env_A_VNC"\n';
                add_header Content-Type "application/json" always;
            }
        }
        server {
            listen 1414;
            location /index.html {
                return 200 '{\n  "nat-gateway-addr": $remote_addr,\n  "destination-port": $server_port,\n  "destination-addr": $server_addr,\n  "nginx-prxy": $host,\n  "status": "200",\n  "message": "OK - Env_A_MQ"\n';
                add_header Content-Type "application/json" always;
            }
        }
        server {
            listen 1416;
            location /index.html {
                return 200 '{\n  "nat-gateway-addr": $remote_addr,\n  "destination-port": $server_port,\n  "destination-addr": $server_addr,\n  "nginx-prxy": $host,\n  "status": "200",\n  "message": "OK - Env_A_MQ"\n';
                add_header Content-Type "application/json" always;
            }
        }
        server {
            listen 1417;
            location /index.html {
                return 200 '{\n  "nat-gateway-addr": $remote_addr,\n  "destination-port": $server_port,\n  "destination-addr": $server_addr,\n  "nginx-prxy": $host,\n  "status": "200",\n  "message": "OK - Env_A_MQ"\n';
                add_header Content-Type "application/json" always;
            }
        }
        server {
            listen 27017;
            location /index.html {
                return 200 '{\n  "nat-gateway-addr": $remote_addr,\n  "destination-port": $server_port,\n  "destination-addr": $server_addr,\n  "nginx-prxy": $host,\n  "status": "200",\n  "message": "OK - Env_A_Mongo"\n';
                add_header Content-Type "application/json" always;
            }
        }
        server {
            listen 60000;
            location /index.html {
                return 200 '{\n  "nat-gateway-addr": $remote_addr,\n  "destination-port": $server_port,\n  "destination-addr": $server_addr,\n  "nginx-prxy": $host,\n  "status": "200",\n  "message": "OK - Env_A_DB"\n';
                add_header Content-Type "application/json" always;
            }
        }
        server {
            listen 60001;
            location /index.html {
                return 200 '{\n  "nat-gateway-addr": $remote_addr,\n  "destination-port": $server_port,\n  "destination-addr": $server_addr,\n  "nginx-prxy": $host,\n  "status": "200",\n  "message": "OK - Env_A_DB"\n';
                add_header Content-Type "application/json" always;
            }
        }
        server {
            listen 60002;
            location /index.html {
                return 200 '{\n  "nat-gateway-addr": $remote_addr,\n  "destination-port": $server_port,\n  "destination-addr": $server_addr,\n  "nginx-prxy": $host,\n  "status": "200",\n  "message": "OK - Env_A_DB"\n';
                add_header Content-Type "application/json" always;
            }
        }
        server {
            listen 445;
            location /index.html {
                return 200 '{\n  "nat-gateway-addr": $remote_addr,\n  "destination-port": $server_port,\n  "destination-addr": $server_addr,\n  "nginx-prxy": $host,\n  "status": "200",\n  "message": "OK - Env_A_RSM"\n';
                add_header Content-Type "application/json" always;
            }
        }
        server {
            listen 2369;
            location /index.html {
                return 200 '{\n  "nat-gateway-addr": $remote_addr,\n  "destination-port": $server_port,\n  "destination-addr": $server_addr,\n  "nginx-prxy": $host,\n  "status": "200",\n  "message": "OK - Env_A_ControlM"\n';
                add_header Content-Type "application/json" always;
            }
        }
        server {
            listen 2370;
            location /index.html {
                return 200 '{\n  "nat-gateway-addr": $remote_addr,\n  "destination-port": $server_port,\n  "destination-addr": $server_addr,\n  "nginx-prxy": $host,\n  "status": "200",\n  "message": "OK - Env_A_ControlM"\n';
                add_header Content-Type "application/json" always;
            }
        }
        server {
            listen 2371;
            location /index.html {
                return 200 '{\n  "nat-gateway-addr": $remote_addr,\n  "destination-port": $server_port,\n  "destination-addr": $server_addr,\n  "nginx-prxy": $host,\n  "status": "200",\n  "message": "OK - Env_A_ControlM"\n';
                add_header Content-Type "application/json" always;
            }
        }
        server {
            listen 9080;
            location /index.html {
                return 200 '{\n  "nat-gateway-addr": $remote_addr,\n  "destination-port": $server_port,\n  "destination-addr": $server_addr,\n  "nginx-prxy": $host,\n  "status": "200",\n  "message": "OK - Env_A WEB"\n';
                add_header Content-Type "application/json" always;
            }
        }
        server {
            listen 80;
            location /index.html {
                return 200 '{\n  "nat-gateway-addr": $remote_addr,\n  "destination-port": $server_port,\n  "destination-addr": $server_addr,\n  "nginx-prxy": $host,\n  "status": "200",\n  "message": "OK - Env_A SLEPOS MGT CONSOLE"\n';
                add_header Content-Type "application/json" always;
            }
        }
        server {
            listen 32700;
            location /index.html {
                return 200 '{\n  "nat-gateway-addr": $remote_addr,\n  "destination-port": $server_port,\n  "destination-addr": $server_addr,\n  "nginx-prxy": $host,\n  "status": "200",\n  "message": "OK - Env_A HAPROXY"\n';
                add_header Content-Type "application/json" always;
            }
        }
        server {
            listen 443;
            location /index.html {
                return 200 '{\n  "nat-gateway-addr": $remote_addr,\n  "destination-port": $server_port,\n  "destination-addr": $server_addr,\n  "nginx-prxy": $host,\n  "status": "200",\n  "message": "OK - Env_A SMT "\n';
                add_header Content-Type "application/json" always;
            }
        }
        server {
            listen 9080;
            location /index.html {
                return 200 '{\n  "nat-gateway-addr": $remote_addr,\n  "destination-port": $server_port,\n  "destination-addr": $server_addr,\n  "nginx-prxy": $host,\n  "status": "200",\n  "message": "OK - Env_A INT QUEUE MGT"\n';
                add_header Content-Type "application/json" always;
            }
        }
        server {
            listen 10000;
            location /index.html {
                return 200 '{\n  "nat-gateway-addr": $remote_addr,\n  "destination-port": $server_port,\n  "destination-addr": $server_addr,\n  "nginx-prxy": $host,\n  "status": "200",\n  "message": "OK - Env_A NDM-PROD"\n';
                add_header Content-Type "application/json" always;
            }
        }
        server {
            listen 10020;
            location /index.html {
                return 200 '{\n  "nat-gateway-addr": $remote_addr,\n  "destination-port": $server_port,\n  "destination-addr": $server_addr,\n  "nginx-prxy": $host,\n  "status": "200",\n  "message": "OK - Env_A NDM-STAGE"\n';
                add_header Content-Type "application/json" always;
            }
        }
        server {
            listen 10040;
            location /index.html {
                return 200 '{\n  "nat-gateway-addr": $remote_addr,\n  "destination-port": $server_port,\n  "destination-addr": $server_addr,\n  "nginx-prxy": $host,\n  "status": "200",\n  "message": "OK - Env_A NDM-DEV"\n';
                add_header Content-Type "application/json" always;
            }
        }
